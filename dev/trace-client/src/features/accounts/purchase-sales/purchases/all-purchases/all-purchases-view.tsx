export function AllPurchasesView({instance}: {instance: string}) {
  console.log("Rendering AllPurchasesView for instance:", instance);
  return (
    <div>
      <h2>All Purchases View</h2>
      {/* Render the details of all purchases here */}
    </div>
  );
}
